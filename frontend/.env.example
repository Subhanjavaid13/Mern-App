# Base URL of the notes API.
# Leave empty in development: Vite proxies "/api" to http://localhost:5000 (see vite.config.js).
# For production builds, point it at the deployed backend, e.g. https://api.example.com/api
VITE_API_URL=
